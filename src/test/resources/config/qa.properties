# QA Environment Configuration
base.url=https://qa.example.com
app.name=Playwright Framework - QA

# Browser Configuration
browser.type=chromium
browser.headless=true
browser.slowmo=0

# Viewport Configuration
viewport.width=1920
viewport.height=1080

# Timeout Configuration
page.timeout=30000
element.timeout=10000
navigation.timeout=30000

# API Configuration
api.base.url=https://api-qa.example.com
api.timeout=30000

# Database Configuration (if needed)
db.url=jdbc:mysql://qa-db.example.com:3306/testdb
db.username=qauser
db.password=qapassword
